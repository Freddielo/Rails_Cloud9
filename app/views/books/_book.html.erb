<li id="book-<%= book.id %>">
  <span class="content"><%= book.name %></span>
  <span class="timestamp">
    Posted <%= time_ago_in_words(book.created_at) %> ago.
    
   
      <%= link_to "delete", book, method: :delete,
                                       data: { confirm: "You sure?" } %>
      <%= link_to "edit", edit_book_path(:bid=>book.id)%>
  </span>
</li>